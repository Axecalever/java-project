pipeline {
	agent {label 'master_node'}	

	stages {
			stage('PRINT') {
				steps {				
					echo '${JOB_NAME}'
				}		
			}
			stage('WRITE') {
				steps {				
					sh 'cat {BUILD_NAME} > build_number'
				}		
			}
			stage('READ') {
				steps {				
					sh 'cat build_number'
				}		
			}

	}
}
